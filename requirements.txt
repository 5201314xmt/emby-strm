# ============================================================
# 缺集管家 - 依赖清单（版本已锁定，保证每次构建结果一致）
# 保持精简，只有 3 个核心依赖，方便小白安装维护
# ============================================================

fastapi==0.138.1        # Web 框架（负责网页和 API）
uvicorn[standard]==0.49.0   # 网页服务器（运行 FastAPI 的引擎）
httpx==0.28.1           # 发 HTTP 请求（访问 MoviePilot / Emby / TMDB）
