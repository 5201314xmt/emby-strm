"""
缺集管家 v2.0 —— 后端应用包

目录说明（按层组织，从上到下不交叉引用）：
  core/     基础设施层：数据库连接、安全、事件总线
  models/   ORM 模型层：数据库表结构定义
  schemas/  数据模式层：API 请求/响应的数据结构
  api/      路由层：    HTTP 接口 + WebSocket
  services/ 业务逻辑层：扫描器、TMDB客户端、缺集分析、订阅管理
  tasks/    任务层：    后台扫描任务编排、定时调度
  utils/    工具函数层
"""
