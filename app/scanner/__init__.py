"""
============================================================
扫描器包 - 所有"找出目前有哪些集"的数据来源都放这里
============================================================
用法（上层代码统一调用 run()，不用关心具体用哪个扫描器）：

    from . import filesystem, emby

    # 文件扫描器（strm 目录）
    result1 = filesystem.scan(paths)

    # Emby 扫描器（可选）
    result2 = emby.scan(url, key)
============================================================
"""
